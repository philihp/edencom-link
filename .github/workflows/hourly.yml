name: Hourly

on:
  workflow_dispatch:
  schedule:
    - cron: '44 * * * *'

jobs:
  dispatch:
    runs-on: ubuntu-latest
    steps:
      - name: Dispatch hourly job to Vercel
        run: |
          curl -sf --max-time 310 \
            -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}" \
            "${{ vars.VERCEL_APP_URL }}/api/cron/hourly"
