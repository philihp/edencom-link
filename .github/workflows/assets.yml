name: Assets

on:
  workflow_dispatch:
  schedule:
    - cron: '26 * * * *'

jobs:
  dispatch:
    runs-on: ubuntu-latest
    steps:
      - name: Dispatch assets job to Vercel
        run: |
          curl -sf --max-time 310 \
            -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}" \
            "${{ vars.VERCEL_APP_URL }}/api/cron/assets"
