name: Structures

on:
  workflow_dispatch:
  schedule:
    - cron: '17 * * * *'

jobs:
  dispatch:
    runs-on: ubuntu-latest
    steps:
      - name: Dispatch structures job to Vercel
        run: |
          curl -sf --max-time 310 \
            -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}" \
            "${{ vars.VERCEL_APP_URL }}/api/cron/structures"
