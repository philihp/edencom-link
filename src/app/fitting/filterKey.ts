// The chosen owner on /fitting, persisted the way the assets and industry
// filters are (see src/app/asset/filterKey.ts) so it survives a reload.
export const OWNER_STORAGE_KEY = 'fittings.ownerId'
